version: "3.3"
services:
  minecraft:
    image: itzg/minecraft-server:latest
    container_name: minecraft
    environment:
      EULA: "TRUE"
    ports:
      - "35565:35565"
    restart: always
    tty: TRUE
    stdin_open: TRUE
    volumes:
      - type: bind
        source: ./
        target: /data
